<div class="container mx-auto py-8" data-controller="alert">
  <div class="grid grid-cols-3 md:grid-cols-2 lg:grid-cols-9 gap-6">
    <% icons = ['fas fa-home', 'fas fa-user', 'fas fa-cog', 'fas fa-envelope', 'fas fa-star', 'fas fa-heart'] %>
    <% icons.each_with_index do |icon, i| %>
      <div class="bg-purple-900 p-6 rounded-lg shadow-lg flex flex-col items-center">
        <i class="<%= icon %> text-6xl text-white mb-4"></i>
        <h2 class="text-2xl font-semibold text-white mb-4">Card <%= i + 1 %></h2>
        <p class="text-gray-200 mb-6 text-center">
          Descrição do card <%= i + 1 %>. Este é um exemplo de card com uma breve descrição.
        </p>
        <button class="px-4 py-2 bg-blue-600 text-white font-semibold rounded-lg shadow-md hover:bg-blue-700 transition-colors duration-300" data-action="click->alert#showAlert" data-alert-title="Card <%= i + 1 %>" data-alert-text="Descrição do card <%= i + 1 %>.">
          Ver mais
        </button>
      </div>
    <% end %>
  </div>
</div>
